# 1.2 环境配置
python -m venv venv 
source venv/bin/activate # Linux/Mac
venv\Scripts\activate # Windows


echo "black >= 23.0.0" > requirements.txt
echo "pytest >= 7.0.0" >> requirements.txt
echo "watchdog >= 3.0.0" >> requirements.txt
ech "pygments >= 2.0.0" >> requirements.txt

pip install -r requirements.txt

git init



# 1.3 基础配置文件
[buil-system]
requirs = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "native-ai-agent"
version = "0.1.0"
description = "原生AI智能体for VS Code"
authors = [{name = "Your Name", email = "your.email@example.com"}]
dependencies = [
    "black >= 23.0.0",
    "pytest >= 7.0.0",
]

[tool.black]
line-length = 88
target-version = ['py310']
